---
title: Thank you
---

<div class="wrapper">
  <div class="content">
    <div class="d-flex flex-column flex-lg-row
    justify-content-around justify-content-lg-around
    align-items-stretch align-items-lg-center">
      <div class="card col-xs-12 col-md-6 my-5 mx-auto">
        <div class="card-body">
          <h2 class="card-title text-center">Thank you!</h2>
          <p class="lead text-center">Be excellent to yourself, your Skiff Mates and your space.</p>
        </div>
      </div>
    </div>
  </div>

<% content_for :javascript do %>
  <script>
    function getParameterByName(name) {
      name = name.replace(/[\[]/, "\\[").replace(/[\]]/, "\\]");
      var regex = new RegExp("[\\?&]" + name + "=([^&#]*)"),
        results = regex.exec(location.search);
      return results === null ? "" : decodeURIComponent(results[1].replace(/\+/g, " "));
    }

    // Get the email address the user has entered
    email = getParameterByName('drip-email');
    // Set the hidden email field
    $(function() {
      $('#drip-email').attr('value', email);
    });
  </script>
<% end %>